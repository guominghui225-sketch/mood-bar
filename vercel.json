{
  "buildCommand": "cd app && npm run build",
  "outputDirectory": "app/dist",
  "framework": "vite",
  "installCommand": "cd server && npm install --no-audit --no-fund && cd ../app && npm install --no-audit --no-fund",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "server/**"
    }
  }
}