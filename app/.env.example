# Mood Bar 前端环境变量模板
# 复制为 .env 用于本地开发，但不要提交到Git
# 生产环境（Vercel）在Dashboard中设置环境变量

# ============ 开发环境 ============
# 本地后端地址
VITE_API_BASE_URL=http://localhost:3007

# ============ 生产环境（Vercel）============
# 方案A：API在同一域名下（推荐）
# 留空，使用相对路径 /api
# VITE_API_BASE_URL=

# 方案B：API在独立域名
# VITE_API_BASE_URL=https://your-api-domain.com

# ============ Vercel部署说明 ============
# 1. 在Vercel Dashboard → Project → Environment Variables
# 2. 添加 VITE_API_BASE_URL 变量
# 3. 值留空（空字符串）或填写你的API域名
# 4. 如果留空，前端会自动请求 /api/* 路径
# 5. 确保已配置 vercel.json 中的路由规则